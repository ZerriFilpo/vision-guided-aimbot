# Shared Definitions

This directory contains common definitions shared between firmware and host applications, such as:

- Serial communication protocols
- Message formats
- Common constants and enums

Entries will be added as the system design evolves.
