# Firmware — Embedded Control

This directory contains the embedded firmware that will run on the microcontroller.

It will include:

- Motor control modules
- Communication interface (UART) to receive commands from the host
- Actuator control for the click mechanism
- Low-level hardware abstraction

Status: awaiting hardware arrival to begin implementation.
